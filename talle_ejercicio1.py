print("bucle de valores cuadrados menores al limite")
limite=int(input("ingrese el limite del bucle"))
inicial=0

while inicial*inicial<limite:
    print(inicial * inicial)
    inicial = inicial + 1

print("valores positivos que son divisibles por 10 y menores que el limite")
limite=int(input("ingrese el limite del bucle"))
inicial=0

while inicial <limite:
    print(inicial)
    inicial = inicial +10

print("potencias de 2 menores al limite")
limite=int(input("ingrese el limite del bucle"))
inicial=0

while 2*inicial <limite:
    print(2*inicial)
    inicial = inicial + 1
